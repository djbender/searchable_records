# Database configuration for SearchableRecords testing

sqlite3:
  adapter: sqlite3
  database: ":memory:"
  
postgresql:
  adapter: postgresql
  database: searchable_records_test
  username: <%= ENV.fetch('POSTGRES_USER', 'postgres') %>
  password: <%= ENV.fetch('POSTGRES_PASSWORD', '') %>
  host: <%= ENV.fetch('POSTGRES_HOST', 'localhost') %>
  port: <%= ENV.fetch('POSTGRES_PORT', 5432) %>
  pool: 5
  timeout: 5000